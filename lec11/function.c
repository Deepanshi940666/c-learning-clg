#include <stdio.h>
/*int sum()  //function
{
    int a;
    int b;
    int c;
    printf("Enter a value of a and b:");
    scanf("%d%d", &a, &b);
    c = a + b;
    printf("Sum=%d", c);
    return 0;
}
int main()
{
    sum(); //calling of function

}*/

// prime no.

/*int prime() // function
{
    int no, i;

    printf("Enter a no.");
    scanf("%d", &no);
    int isPrime = 1;
    if (no <= 1)
    {
        printf("Not Prime No.");
        return 0;
    }
    for (i = 2; i < no; i++)
    {
        if (no % i == 0)
        {
            isPrime = 0;
            break;
        }
    }
    if (isPrime)
    {
        printf("Prime");
    }
    else
    {
        printf(" Not prime");
    }
}
int main()
{
    prime();
}*/

/*
int no(int a, int b){   //parameter
    // scanf("%d%d",&a,&b);
    printf("you have entered:\n%d\n%d",a,b);
    return 0;
}

int main()
{
    no(6,3);  //arguments
}*/

/*odd or even
int even_odd(int i)
{
    if (i % 2 == 0)
        printf("even");
    else
        printf("odd");
    return 0;
    printf("ohh nooo"); // after writing the return statement the code will stop so it do not print any statement after it
}
int main()
{
    even_odd(256);
}*/

/*int sum()  //function
{
    int a=5;
    int b=7;
    int c;
    c = a + b;
    return c;
}
int main()
{
  int data=  sum(); //calling of function
  printf("%d",data);

}*/
// int main(){

//     int sum = 0, n;
//     printf("Enter a no.");
//     scanf("%d", &n);
//     while (n!= 0)
//     {
//         int lastDigit = n % 10;
//         sum = sum + lastDigit;
//         n = n / 10;
//     }
//     printf("sum=%d", sum);
//     return 0;
// }
